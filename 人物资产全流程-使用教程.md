# 人物资产全流程使用教程（定妆照 + 四联图）

> 一套节点 + 一张联动工作流，一次队列产出角色全部参考图：
> **基础定妆照 → 造型定妆照 → 证件照/正/侧/背 → 1920×1080 四联图**
> 产物直接进 `资产\角色\`，文件名对齐导演台资产库和 wumu-movie 引擎的引用语法。

---

## 一、三个产物层级（先懂这个再上手）

| 层级 | 是什么 | 怎么来的 | 用在哪 |
|---|---|---|---|
| ① 基础定妆照 | 穿内衣白底全身照，锁**脸/发型/体型/肤色** | FLUX.2 Dev 文生图 | 换装的参考底图，一般不直接进视频 |
| ② 造型定妆照 | 同一张脸，换上具体服装 | ① 参考 + 服装提示词 | 四联图的输入；场景合成的角色参考 |
| ③ 四联图 | 证件照(大)+全身正面+侧视+后视 合成 1920×1080 | Klein 四链生成，**纯白背景** | **导演台资产库正式角色参考**，引擎 `角色名@造型名` 引用 |

---

## 二、准备

1. **完全重启 ComfyUI**（新节点代码需要重启才注册；改过节点后也要重启）
2. 打开工作流：侧边栏 → `人物资产全流程-定妆照+四联图.json`
   （找不到就把 `F:\comfyui\ComfyUI\ComfyUI\user\default\workflows\人物资产全流程-定妆照+四联图.json` 直接拖到画布上）

画布结构：

```
 📖总说明                    📖四联图工坊手册
 ┌─ 定妆照工坊(FLUX.2 Dev) ─┐        ┌─ 四联图工坊(Klein) ─┐
 │ 基础定妆照 → 预览        │        │                     │
📖工坊手册                  │        │                     │
 │ 造型1定妆照 ─────────────┼──slot1─┤   四联图1 → 预览     │
 │ 造型2~5定妆照 ───────────┼──slotN─┤   四联图2~5          │
 └──────────────────────────┘        └─────────────────────┘
```

画布上有三张便签：一张总说明 + 每个节点旁边各一张 📖 参数手册（每个参数的含义/怎么设/默认值）。

---

## 三、使用步骤（联动模式）

### 第 1 步：填左节点（定妆照工坊）

| 参数 | 填什么 |
|---|---|
| 角色 | 中文角色名，如 `周屿`（文件命名用） |
| 人物英文描述 | **只写身份特征**：国籍人种、年龄体型、脸型发型、肤色、特殊标记。国籍写最前面（防人种漂移）。❌ 不要写服装 |
| 造型槽 | 要几个造型就启用几个：勾选 + 填造型中文名（如 `睡衣`）+ 服装提示词 |
| use_turbo | 测试期建议开（8 步快 2.5 倍，画质略降）；正式出片关掉用 20 步 |

服装提示词模板（Kontext 格式，直接抄）：

```
Edit the reference photo: the SAME person with identical face, hairstyle,
body shape and skin tone, now wearing [这里写服装，如 a navy blue silk pajama set
with slim fit pants]. Standing facing the camera, full body visible head to toe,
arms relaxed at sides with hands visible, seamless pure white studio background,
nothing else in frame, soft even frontal studio lighting, photorealistic,
sharp focus. no text, no watermark, no logo.
```

> 也可以直接把 wumu-movie 引擎生成的完整基础定妆照提示词粘进「人物英文描述」，节点会自动剥离模板只留身份核心，两种填法都行。

### 第 2 步：填右节点（四联图工坊）

只需要对齐两个东西：

- **角色名** = 左节点角色名
- **造型名** = 左节点启用的造型名（槽位一一对应）

其余参数保持默认（Klein 模型、4 步、1920×1080 画布）就是正确配置。

### 第 3 步：Queue

- 左节点先跑：1 张基础照 + N 张造型照（每张约 1~2 分钟）
- 右节点后跑：每个造型四条链（证件照/正视/侧视/后视，各 4 步 Klein，很快）
- 显存不够也不怕：ComfyUI 自动在两个模型间换入换出，只是稍慢

### 第 4 步：收产物

全部自动保存到 `F:\AI\MINIMAXH3\资产\角色\`（每个造型 6 个文件）：

```
周屿_基础定妆照.png          ← 第①层（全角色仅一张）
周屿_睡衣_定妆照.png          ← 第②层（造型照本体）
周屿_睡衣_证件照.png          ┐
周屿_睡衣_正面图.png          ├ 第③层（save_views 开时保存）
周屿_睡衣_侧面图.png          │
周屿_睡衣_背面图.png          ┘
周屿_睡衣_四联图.png          ← 最终合成：证件照(大)+正面+侧面+背面
```

**save_views 保持开启**——导演台分段引用 `角色名@造型名` 时引擎展开的就是 `正面图/侧面图/背面图` 这三个文件名。

四联图版式：证件照约 745px 高（方形大图），三张全身照约 637px 高，白底垂直居中，超宽自动等比缩小。

---

## 四、手动模式（不跑定妆照，只用已有图做四联图）

适合：重做某个造型的四联图、用旧图补四联图。

1. 删掉右节点上的连线（右键连线 → 删除，或直接删左节点）
2. 加一个 **LoadImage** 节点，上传造型定妆照
3. 把 LoadImage 输出连到四联图工坊的 **slot1_image**
4. 对齐右节点的 角色/造型名 → Queue

连几个槽就跑几个造型；没连的槽自动跳过。

---

## 五、参数速查

### 定妆照工坊（左）

| 参数 | 默认 | 说明 |
|---|---|---|
| 风格 | 写实电影 | boreal LoRA；其他风格见节点内下拉 |
| style_strength | 0.8 | 写实类 0.6~0.8 |
| steps / guidance | 20 / 4.0 | FLUX.2 标准值；turbo 时步数改 8 |
| 宽×高 | 832×1216 | 竖构图全身照标准 |
| seed | 随机 | 造型N 自动用 seed+N×10 |

### 四联图工坊（右）

| 参数 | 默认 | 说明 |
|---|---|---|
| steps | 4 | Klein 标准值，不用动 |
| seed | 随机 | 每槽 +100，四链再 +1/+10/+20/+30 |
| canvas | 1920×1080 | 四联图画布，导演台横版标准 |
| front_h / side_h | 960 / 820 | 证件照/全身照高度比；适配后实际约 745 / 637 |
| save_views | 开 | 另存四个单视角图（引擎引用需要） |

---

## 六、常见问题

**Q1：右节点报「没有可用输入」**
没连任何图。联动模式检查左节点连线；手动模式连 LoadImage。
注意：左节点**没启用的造型槽**输出的是占位黑图，右节点会自动跳过（正常行为，不是 bug）。

**Q2：视角图背景发灰？**
v3 起正/侧/后三条链的提示词都已锁定「纯白色摄影棚背景」，与证件照一致。若你还在跑旧版产物，重启 ComfyUI 重新生成即可。仍有个别图偏灰的话换 seed 重抽（4 步 Klein 有抽卡波动）。

**Q3：侧视图脸转回来了 / 后视图能看到脸**
换 seed 重跑（四联图工坊的 seed 点随机再 Queue）。这是 Klein 4 步采样的固有抽卡问题，提示词里已加「绝对侧面剪影」类约束。

**Q4：造型照的脸和基础照不一致**
看左节点：确认服装提示词是 Kontext 格式（`the SAME person...`开头），且 use_turbo 关掉重跑。FLUX.2 参考链下脸一致性远好于旧 FLUX.1 方案，若仍漂移换 seed。

**Q5：显存爆了**
16G 卡正常情况不会。若手动改大分辨率后 OOM：定妆照保持 832×1216，四联图四链分辨率是节点内定的（1024 竖），不要试图改。

**Q6：模型下拉是空的/选错**
定妆照工坊三项分别应为 `flux2_dev_fp8mixed` / `mistral_3_small_flux2_bf16` / `full_encoder_small_decoder`；四联图工坊为 `flux-2-klein-9b-kv-fp8` / `qwen_3_8b_fp8mixed` / `full_encoder_small_decoder`。下拉已按关键词过滤，默认值即正确。

**Q7：language 下拉是干什么的？**
输出语言：**English** 时落盘目录变 `assets\characters\`、文件后缀变 `_id/_front/_side/_back/_quadtych`，四联图的四视角生成提示词和 info/日志也切英文。**跑 wumu-movie / 导演台流水线必须保持「中文」**——引擎按中文文件名展开 `角色名@造型名` 引用。参数提示语言是全局设置：切换后刷新页面生效。

---

## 七、和导演台/wumu-movie 的衔接

1. `资产\角色\` 下的 `角色_造型_四联图.png` 拖进导演台 7.0 创作页资产库
2. 分镜提示词里引用：`角色名@造型名`（引擎展开为正/侧/背三文件）
3. 台词锁、身份锚定句等规则见 wumu-movie 技能的 04-资产 章节手册
